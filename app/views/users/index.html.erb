<div class="page-header">
  <h1>
    Manager
     <a href="/users/new" class="btn btn-lg btn-success">New Manager</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>User</th>
        <th>Actions</th>
      </tr>

      <% @users.each do |user| %>
      <tr>
        <td><%= user.username %></td>
        <td>
          <a href="/users/<%= user.id %>" class="btn btn-primary">Show</a>
          <% if current_user.id == user.id  %>
              <a href="/properties/new" class="btn btn-warning"> add a new property</a>
              <% end %>

        </td>

      </tr>
      <% end %>
    </table>
  </div>
  </div>
